package factory_pattern.zones;

import factory_pattern.Zone;

public class ZoneUSCentral extends Zone {
    public ZoneUSCentral(String displayName, String offset) {
        super(displayName, offset);
    }
}
