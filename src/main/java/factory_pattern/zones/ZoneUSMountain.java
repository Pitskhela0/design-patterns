package factory_pattern.zones;

import factory_pattern.Zone;

public class ZoneUSMountain extends Zone {
    public ZoneUSMountain(String displayName, String offset) {
        super(displayName, offset);
    }
}
