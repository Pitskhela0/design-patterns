package factory_pattern.zones;

import factory_pattern.Zone;

public class ZoneUSPacific extends Zone {
    public ZoneUSPacific(String displayName, String offset) {
        super(displayName, offset);
    }
}
